# URL del backend — en local apunta al servidor Express
NEXT_PUBLIC_API_URL=http://localhost:3001

# Measurement ID de GA4 (empieza por "G-"). Solo se carga en el navegador
# tras aceptar cookies analíticas en el banner — déjalo vacío para desactivar
# Google Analytics por completo.
NEXT_PUBLIC_GA_ID=
