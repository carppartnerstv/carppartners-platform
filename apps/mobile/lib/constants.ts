// Etiqueta visible a partir de `role` — única fuente de verdad, no hay texto
// libre por persona. Mismo mapa que el resto del proyecto (web incluida).
export const ROLE_LABELS: Record<'socio' | 'crew', string> = {
  socio: 'Socio',
  crew: 'Miembro de la crew',
};
