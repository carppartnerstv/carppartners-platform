import { colors, textStyles, spacing, radii, shadows } from '../theme';

// Single dark theme — light mode is Phase 2
export function useTheme() {
  return { colors, textStyles, spacing, radii, shadows };
}
